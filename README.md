# SEC Filing Tracker (GitHub Actions)
---
10-K 是什麼
年報（年度財報）：最完整、最長的一份，通常包含
全年財務報表與附註
風險因素、營運概況、管理層討論（MD&A）
內控、重大訴訟等
對美國公司最常見。

10-Q 是什麼
季報（季度財報）：一年通常 3 份（Q1/Q2/Q3），內容比 10-K 短，但還是有財務報表與重點說明。

8-K 是什麼
重大事件快報：不是固定週期的財報，重點是「發生重大事情就要快點公告」，常見像：
財報公布（有時候會附新聞稿/簡報）
併購、重大合約、訴訟
高層異動、裁員、破產風險
股票回購、債券發行、重大會計更動等

20-F 是什麼
外國公司（Foreign Private Issuer）的年報：概念上接近 10-K，但給「外國發行人」用（像 ADR/海外公司在美國上市）。

6-K 是什麼
外國公司（Foreign Private Issuer）的重大資訊/臨時公告：概念上接近 8-K，但給外國發行人用。

所以你會看到像 TSM（ADR） 常出現 6-K/20-F，而不是 8-K/10-K。

----
每週自動檢查你追蹤的美股清單，在 SEC EDGAR 上「最近 30 天」是否有新的 filing（10-K, 10-Q, 20-F, 8-K, 6-K），並輸出成週報 Markdown。

- ✅ 每週六 12:00 台灣時間自動跑（UTC 04:00）
- ✅ 支援手動觸發（workflow_dispatch）
- ✅ 不下載 PDF，只輸出 SEC 原始文件 URL
- ✅ 每支股票都有一段：有就列出、沒有就顯示 `No new filings in last 30 days`
- ✅ 自動清理 30 天前的舊報告（僅限 `reports/` 下的 `YYYY-MM-DD.md`）

---

## Repo 結構

```

.
├─ .github/workflows/sec-filing-tracker.yml
├─ config/tickers.json
├─ scripts/sec_filing_tracker.py
├─ reports/                # 週報輸出位置（自動產生）
└─ index.md                # 週報索引（自動更新）

````

> 建議 `reports/` 放一個空檔 `reports/.gitkeep`，讓資料夾可以被 commit。

---

## 設定股票清單

編輯 `config/tickers.json`：

```json
[
  {"ticker": "NVDA", "name": "NVIDIA"},
  {"ticker": "GE", "name": "GE Aerospace"},
  {"ticker": "AAPL", "name": "Apple"}
]
````

* `ticker`: 股票代號（用來對照 SEC 的 CIK）
* `name`: 報告顯示用名稱
* 不在 SEC 的（非美股/非 SEC 申報）會顯示 `No new filings...`

---

## User-Agent（很重要）

SEC 要求請求頭必須包含可聯絡 email，格式為：

```
ProjectName your.email@example.com
```

請在 `scripts/sec_filing_tracker.py` 裡設定，例如：

```python
USER_AGENT = "SEC-Filing-Tracker mis23ms@users.noreply.github.com"
```

---

## 產出內容

每次執行會產生：

* `reports/YYYY-MM-DD.md`：當天週報
* `index.md`：自動列出所有週報連結（新到舊）

週報格式示例：

```md
# SEC Filing Tracker — 2026-02-10

## AAPL — Apple
- 10-Q | Filed: 2026-01-30 | 🔗 [Link](https://www.sec.gov/Archives/edgar/data/...)

## XXXX — Some Company
_No new filings in last 30 days_
```

> `Filed:` 是「提交到 EDGAR 的日期」，不是財報涵蓋期間。

---

## GitHub Actions

Workflow 檔案：`.github/workflows/sec-filing-tracker.yml`

觸發方式：

* 排程：每週六 04:00 UTC（= 台灣 12:00）
* 手動：GitHub → Actions → SEC Filing Tracker → Run workflow

---

## GitHub Pages（可選）

如果你想把 `index.md` 當作網頁入口：

GitHub → Settings → Pages

* Source: Deploy from a branch
* Branch: `main`
* Folder: `/(root)`

前提：你的 `index.md` 在 repo 根目錄（本 repo 方案就是這樣）。

---

## 本機測試（可選）

```bash
pip install requests
python scripts/sec_filing_tracker.py
```

執行後會更新 `reports/` 和 `index.md`。

---

## 限制

* 只抓指定表格類型：10-K / 10-Q / 20-F / 8-K / 6-K
* 只列 URL，不下載 PDF
* 不做額外評分/過濾/門檻判斷

```

